using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NLevel_Handler : MonoBehaviour
{
    // Start is called before the first frame update
    void Level_Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
